# @package _group_

train:
  _target_: torch.utils.data.sampler.BatchSampler
  sampler:
    _target_: torch.utils.data.RandomSampler
    data_source: ???
  batch_size: 16
  drop_last: false

val:
  _target_: torch.utils.data.sampler.BatchSampler
  sampler:
    _target_: torch.utils.data.SequentialSampler
    data_source: ???
  batch_size: 16
  drop_last: false

test:
  _target_: torch.utils.data.sampler.BatchSampler
  sampler:
    _target_: SequentialSampler
    data_source: ???
  batch_size: 16
  drop_last: false